# Personal API tokens, one per person, from Todoist → Settings → Integrations → Developer.
# Used only by scripts/sync.mjs, which runs on a schedule. Never shipped to the browser.
TODOIST_TOKEN_A=
TODOIST_TOKEN_B=
